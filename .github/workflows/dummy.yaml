---
name: Dummy

on:
  workflow_dispatch:

jobs:
  test:
    name: Dummy ${{ matrix.os }}
    permissions:
      contents: read
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-latest
          - ubuntu-latest
    steps:
      - run: echo "Hi Duncan!"
